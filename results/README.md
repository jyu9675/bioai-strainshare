# results/ (runtime — contents are gitignored)

Pipeline outputs land here: inStrain profiles (`*.IS`), `compare.IS/`, merged MetaPhlAn
tables, the tagged-pairs TSVs, and `figures/`. None are committed.

For a committed reference of what finished output looks like, see [`../example/`](../example),
which holds the synthetic smoke-test outputs.
